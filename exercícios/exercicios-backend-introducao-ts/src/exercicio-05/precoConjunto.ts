function solucao(
  precoPeca1: number,
  precoPeca2: number,
  precoPeca3: number,
  quantidade: number
): number {
  
  return ((precoPeca1 + precoPeca2 + precoPeca3) * quantidade)

}

export default solucao;
